# Base URL for the AI Recruiter API. Must match the port ASP.NET Core's "http" launch
# profile actually listens on (see src/AIRecruiter.API/Properties/launchSettings.json).
# The API runs plain HTTP in Development on purpose — see Program.cs for why.
VITE_API_BASE_URL=http://localhost:5087/api
